\paragraph{Piccolo teorema di Fermat} Se $p$ è primo, allora, $\forall a$, 
\[
    a^p \equiv a \pmod p.
    \]
    Equivalentemente, se $p$ è primo e $a$ è coprimo con $p$, ovvero $\gcd (a,p)=1$, allora 
    \[a^{p-1} \equiv 1 \pmod p.
    \] 
    
    % (Tale teorema è alla base del test di primalità.) 

\begin{proof} (Dimostrazione come corollario del teorema di Lagrange sui gruppi)\\
    Essendo $p$ primo, gli interi da $1$ a $p-1$, quindi escluso lo zero, formano un gruppo moltiplicativo $G \pmod p$, ovvero $(\mathbb{Z}_p^*, \cdot)$, avente come elemento identità il numero $1$. \\ L'ordine del gruppo ($\#$ elementi) è pari a $n-1$. \\ L'ordine di un elemento qualsiasi nel gruppo, $a \in G$, è il più piccolo esponente $i$ tale che $a^i$ sia l'identità. \\ Ma, dal teorema di Lagrange sui gruppi, l'ordine dell'elemento deve dividere l'ordine del gruppo. \\ Ciò significa che $a^{p-1} \equiv 1 \pmod p$ e che l'esponente deve essere un multiplo di $p-1$. Quindi, elevando qualsiasi elemento del gruppo a $p-1$, si ottiene sempre $1 \pmod p$. 
\end{proof}

\paragraph{Generalizzazioni}
\begin{itemize}
    \item se $p$ è primo e $n,m >0$ con $m \equiv n \pmod {p-1}$, allora, $\forall a$, $a^m \equiv a^n \pmod p$.
    \item \textbf{Teorema di Eulero} $\forall$ modulo $n$, e $ \forall a$, coprimo con $n$: $a^{\phi(n)} \equiv 1 \pmod n$.
\end{itemize}

\paragraph{Corollario} Se $a \in \mathbb{Z}_n^*$, allora $a^{\phi(n)} \equiv 1 \pmod n$. \label{Corollario1}

\begin{proof}
    $\mathbb{Z}_n^*$ è un gruppo moltiplicativo di ordine $n$.
\end{proof}

\paragraph{Corollario (Fermat)} Sia $p$ primo e $a \in \mathbb{Z}_p$. Allora $a^p \equiv a \pmod p$. 

\begin{proof}
    Se $p$ è primo, allora $\phi(p)=p-1$. Quindi:
    \begin{itemize}
        \item per $b \not \equiv 0 \pmod p$, il risultato deriva da \ref{Corollario1}
        \item per $b \equiv 0 \pmod p$, poiché $0^p \equiv 0 \pmod p$, è vero.
    \end{itemize}
\end{proof}

\subsection{Introduzione alla Crittografia Asimmetrica}
Per evitare che Alice e Bob si mettano d'accordo in anticipo su una chiave comune, con il riscio di comunicare su un canale insicuro, nasce l'idea di crittografia a chiave pubblica. Alice usa una chiave pubblica $e_K$, conosciuta a chiunque per la cifratura, mentre Bob decifra con la sua chiave privata $d_K$, posseduta solo da lui. 

\vspace{0.2cm} \noindent Un'osservazione è che tale tipo di crittografia non può mai fornire una sicurezza incondizionata, perché un avversario, osservando il ciphertext $y$, può cifrarlo usando $e_k$ finché non trova l'unica $x$ tale che $y=e_K(x)$.\\ Quindi decifrare dovrebbe essere difficile, tranne che per Bob.

\vspace{0.2cm} \noindent Potremmo prendere funzioni facili da calcolare, ma difficili da invertire come funzioni ``one-way'' iniettive, ma non è sufficiente, perché Bob dovrebbe decifrare in modo efficiente. È necessario quindi che Bob possieda una ``trapdoor'', una conoscenza segreta aggiuntiva, ovvero $K$, che gli permetta di invertire facilmente $e_K$. \\ È utile specificare una famiglia di funzioni ``one-way trapdoor'' $\mathcal{F}$; una funzione $f \in \mathcal{F}$ viene scelta casualmente ed usata per la cifratura pubblica; l'inversa $f^{-1}$ è la privata. 

%fatto paragrafo 6.1

\subsection{Algoritmo Euclideo, Identità di Bezout e Teorema cinese del resto}
Si presenta ora l'algoritmo euclideo utilizzato per calcolare il valore $\gcd(a,b)$ per due dati interi. Si inizia impostando $r_0 = a$ e $r_1 = b$:
\begin{align*}
    r_0 &= q_1r_1+r_2, \qquad 0 < r_2 < r_1\\
    r_1 &= q_2r_2+r_3, \qquad 0 < r_3 < r_2\\
    \quad \vdots & \quad \vdots  \quad \vdots \\
    r_{m-2} &= q_{m-1}r_{m-1} + r_m, \qquad 0 < r_m < r_{m-1}\\
    r_{m-1} &= q_{m}r_{m}. &
\end{align*}

Si presenta di seguito l'algoritmo iterato in cui si divide $r_{i-1}$ per $r_{i}$ per ogni $i=1,\dots,m$, dall'algoritmo è chiaro che 
\[
\gcd(r_0,r_1) = \gcd(r_1,r_2) = \ldots = \gcd(r_{m-1},r_m) = r_m
\] 

\input{algorithms/euclidean.tex}



% l'inversa moltiplicativa degli elementi in ${\mathbb{Z}_n}^*$. 