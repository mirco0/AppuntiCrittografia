\begin{algorithm}[H]

    \SetAlgoVlined
    \SetAlgoLined
    \label{alg:collision}

    \KwIn{Parametri $h, Q$}
    \KwOut{Coppia $(x,x')$ o errore}
    
    \caption{Trova Collisione($h,Q$)}
    scegli $\mathcal{X}_0 \subseteq \mathcal{X}$ tale che $|\mathcal{X}| = Q$\;
    \ForEach{$x \in \mathcal{X}_0$}{
        $y_x \gets h(x)$\;
    }
    \If{$y_x = y_{x'}$ per qualche $x' \neq x$}{
        \Return{$(x,x')$}\;
    }
    \Return{Errore}\;
\end{algorithm}
